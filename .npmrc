# pnpm supply-chain: allow packages regardless of publish age
# (lockfile contains recent transitive deps like caniuse-lite & electron-to-chromium
#  that auto-update frequently; a minimum age policy adds no value here)
minimum-release-age=0
